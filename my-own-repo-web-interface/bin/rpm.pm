#!/usr/bin/perl -w

use DBI;

sub add_package
{
#    return ($pkgname, $pkgver, $pkgdesc, $url, $builddate, $packager, $size, $arch, $license, $depend, $backup, $filelist);
    return ();
}

return TRUE;
