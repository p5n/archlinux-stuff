# Contributor: Sergej Pupykin <ps@lx-ltd.ru>
pkgname=jabberd1-pam
pkgver=20020507
pkgrel=1
pkgdesc="jabberd1 pam support"
url="http://www.jabber.org/old-dl/contrib/jabber-pam.README"
license="GPL"
depends=(jabberd1)
conflicts=()
replaces=()
backup=()
install=
source=(http://www.jabber.org/old-dl/contrib/jabber-pam.tgz)

build() {
/bin/true
#  cd $startdir/src/irc-transport
#  ./configure --prefix=/usr
#  make || return 1
#  install -D -m 0755 ./src/irc_transport.so $startdir/pkg/usr/lib/jabberd/irc_transport.so && \
#  install -D -m 0644 ./src/irc_trans.xml $startdir/pkg/etc/jabberd/irc_trans.xml
}
